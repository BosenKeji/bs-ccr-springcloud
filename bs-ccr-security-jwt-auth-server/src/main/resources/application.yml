server:
  port: 8100
spring:
  application:
    name: bs-ccr-security-jwt-auth-server
  main:
    allow-bean-definition-overriding: true
  cloud:
    nacos:
      discovery:
        server-addr: nacos-dev:8848
mybatis:
  mapper-locations: classpath*:mapping/*.xml
