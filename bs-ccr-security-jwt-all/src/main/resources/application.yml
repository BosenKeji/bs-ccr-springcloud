server:
  port: 8100
mybatis:
  mapper-locations: classpath*:mapping/*.xml
spring:
  application:
    name: bs-ccr-security-jwt-server
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848